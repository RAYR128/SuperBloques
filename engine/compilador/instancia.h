#pragma once

#include <string>

// Un objeto es una declaracion de un objeto en la escena, los objetos contienen "bloques" los cuales son compilados a scripts de behavior que indican como
// se comporta el objeto en la escena. Un objeto puede ser un enemigo, un item, un bloque, etc. La programacion es libre para el usuario.
class ObjetoEscena {
};